모든 경우의 수를 확인 해야할 때
for 로는 확인 불가한 경우 (깊이가 달라질 때)

tree 방식으로 한다. (재귀 함수)
def recur(num):
    if num==N:
        return
    for 1~N :
        결과 값 추가
        recur(num)

1. 아이디어
1부터 N중에 하나를 선택한 뒤,
다음 1부터 N 까지 선택할 떄 이미 선택한 값이 아닌경우 선택
M개를 선택할 경우 프린트

2. 시간복잡도
N^N: 중복이 가능 N=8까지 가능
N! : 중복이 불가 N=10까지 가능

3. 자료구조
방문 여부 bool[]
선택한 값 입력 배열 int []